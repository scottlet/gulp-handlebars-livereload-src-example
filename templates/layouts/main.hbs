<!DOCTYPE html>
<html lang="{{locale}}"{{#if className}} class="{{className}}"{{/if}}>
  <head>
    <meta charset="utf-8" />
    <title>{{__ data.pageTitle}}</title>
    <link rel="stylesheet" href="{{path 'css/style.css'}}" />
    <meta name="viewport" content="user-scalable=0, initial-scale=1.0,minimal-ui" />
    <meta name="author" content="Scott van Looy" />
  </head>
  <body>
      <nav>
        {{#eq '/de/' (getLocale)}}
          <a href="/">EN</a>
          DE
        {{else}}
          EN
          <a href="/de">DE</a>
        {{/eq}}
      </nav>
      {{#> content}}{{/content}}
      <script type="text/javascript" src="{{path 'js/app.min.js'}}"></script>
      <script type="text/javascript" src="{{path 'js/app2.min.js'}}"></script>
  </body>
  </html>
