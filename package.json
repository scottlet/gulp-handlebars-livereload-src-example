{
  "name": "gulp-handlebars-livereload-src",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "eslint './**/*.{js,jsx}'",
    "lint:scss": "stylelint '**/*.scss'",
    "lint:type-check": "tsc --project jsconfig.json --noEmit  --maxNodeModuleJsDepth 0",
    "validate": "npm run lint && npm run lint:type-check && npm run lint:scss"
  },
  "browserslist": [
    "> 2%",
    "last 1 version",
    "IE 11",
    "not dead"
  ]
}
